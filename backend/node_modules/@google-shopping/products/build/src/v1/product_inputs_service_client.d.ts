import type * as gax from 'google-gax';
import type { Callback, CallOptions, Descriptors, ClientOptions } from 'google-gax';
import * as protos from '../../protos/protos';
/**
 *  Service to use ProductInput resource.
 * @class
 * @memberof v1
 */
export declare class ProductInputsServiceClient {
    private _terminated;
    private _opts;
    private _providedCustomServicePath;
    private _gaxModule;
    private _gaxGrpc;
    private _protos;
    private _defaults;
    private _universeDomain;
    private _servicePath;
    private _log;
    auth: gax.GoogleAuth;
    descriptors: Descriptors;
    warn: (code: string, message: string, warnType?: string) => void;
    innerApiCalls: {
        [name: string]: Function;
    };
    pathTemplates: {
        [name: string]: gax.PathTemplate;
    };
    productInputsServiceStub?: Promise<{
        [name: string]: Function;
    }>;
    /**
     * Construct an instance of ProductInputsServiceClient.
     *
     * @param {object} [options] - The configuration object.
     * The options accepted by the constructor are described in detail
     * in [this document](https://github.com/googleapis/gax-nodejs/blob/main/client-libraries.md#creating-the-client-instance).
     * The common options are:
     * @param {object} [options.credentials] - Credentials object.
     * @param {string} [options.credentials.client_email]
     * @param {string} [options.credentials.private_key]
     * @param {string} [options.email] - Account email address. Required when
     *     using a .pem or .p12 keyFilename.
     * @param {string} [options.keyFilename] - Full path to the a .json, .pem, or
     *     .p12 key downloaded from the Google Developers Console. If you provide
     *     a path to a JSON file, the projectId option below is not necessary.
     *     NOTE: .pem and .p12 require you to specify options.email as well.
     * @param {number} [options.port] - The port on which to connect to
     *     the remote host.
     * @param {string} [options.projectId] - The project ID from the Google
     *     Developer's Console, e.g. 'grape-spaceship-123'. We will also check
     *     the environment variable GCLOUD_PROJECT for your project ID. If your
     *     app is running in an environment which supports
     *     {@link https://cloud.google.com/docs/authentication/application-default-credentials Application Default Credentials},
     *     your project ID will be detected automatically.
     * @param {string} [options.apiEndpoint] - The domain name of the
     *     API remote host.
     * @param {gax.ClientConfig} [options.clientConfig] - Client configuration override.
     *     Follows the structure of {@link gapicConfig}.
     * @param {boolean} [options.fallback] - Use HTTP/1.1 REST mode.
     *     For more information, please check the
     *     {@link https://github.com/googleapis/gax-nodejs/blob/main/client-libraries.md#http11-rest-api-mode documentation}.
     * @param {gax} [gaxInstance]: loaded instance of `google-gax`. Useful if you
     *     need to avoid loading the default gRPC version and want to use the fallback
     *     HTTP implementation. Load only fallback version and pass it to the constructor:
     *     ```
     *     const gax = require('google-gax/build/src/fallback'); // avoids loading google-gax with gRPC
     *     const client = new ProductInputsServiceClient({fallback: true}, gax);
     *     ```
     */
    constructor(opts?: ClientOptions, gaxInstance?: typeof gax | typeof gax.fallback);
    /**
     * Initialize the client.
     * Performs asynchronous operations (such as authentication) and prepares the client.
     * This function will be called automatically when any class method is called for the
     * first time, but if you need to initialize it before calling an actual method,
     * feel free to call initialize() directly.
     *
     * You can await on this method if you want to make sure the client is initialized.
     *
     * @returns {Promise} A promise that resolves to an authenticated service stub.
     */
    initialize(): Promise<{
        [name: string]: Function;
    }>;
    /**
     * The DNS address for this API service.
     * @deprecated Use the apiEndpoint method of the client instance.
     * @returns {string} The DNS address for this service.
     */
    static get servicePath(): string;
    /**
     * The DNS address for this API service - same as servicePath.
     * @deprecated Use the apiEndpoint method of the client instance.
     * @returns {string} The DNS address for this service.
     */
    static get apiEndpoint(): string;
    /**
     * The DNS address for this API service.
     * @returns {string} The DNS address for this service.
     */
    get apiEndpoint(): string;
    get universeDomain(): string;
    /**
     * The port for this API service.
     * @returns {number} The default port for this service.
     */
    static get port(): number;
    /**
     * The scopes needed to make gRPC calls for every method defined
     * in this service.
     * @returns {string[]} List of default scopes.
     */
    static get scopes(): string[];
    getProjectId(): Promise<string>;
    getProjectId(callback: Callback<string, undefined, undefined>): void;
    /**
     * [Uploads a product input to your Merchant Center
     * account](/merchant/api/guides/products/add-manage#add_a_product). You
     * must have a products [data
     * source](/merchant/api/guides/data-sources/api-sources#create-primary-data-source)
     * to be able to insert a product. The unique identifier of the data source is
     * passed as a query parameter in the request URL.
     *
     * If a product input with the same contentLanguage, offerId, and dataSource
     * already exists, then the product input inserted by this method replaces
     * that entry.
     *
     * After inserting, updating, or deleting a product input, it may take several
     * minutes before the processed product can be retrieved.
     *
     * @param {Object} request
     *   The request object that will be sent.
     * @param {string} request.parent
     *   Required. The account where this product will be inserted.
     *   Format: `accounts/{account}`
     * @param {google.shopping.merchant.products.v1.ProductInput} request.productInput
     *   Required. The product input to insert.
     * @param {string} request.dataSource
     *   Required. The primary or supplemental product data source name. If the
     *   product already exists and data source provided is different, then the
     *   product will be moved to a new data source. For more information, see
     *   [Create a primary data
     *   source](/merchant/api/guides/data-sources/api-sources#create-primary-data-source).
     *
     *   Only API data sources are supported.
     *
     *   Format: `accounts/{account}/dataSources/{datasource}`. For example,
     *   `accounts/123456/dataSources/104628`.
     * @param {object} [options]
     *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
     * @returns {Promise} - The promise which resolves to an array.
     *   The first element of the array is an object representing {@link protos.google.shopping.merchant.products.v1.ProductInput|ProductInput}.
     *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods | documentation }
     *   for more details and examples.
     * @example <caption>include:samples/generated/v1/product_inputs_service.insert_product_input.js</caption>
     * region_tag:merchantapi_v1_generated_ProductInputsService_InsertProductInput_async
     */
    insertProductInput(request?: protos.google.shopping.merchant.products.v1.IInsertProductInputRequest, options?: CallOptions): Promise<[
        protos.google.shopping.merchant.products.v1.IProductInput,
        protos.google.shopping.merchant.products.v1.IInsertProductInputRequest | undefined,
        {} | undefined
    ]>;
    insertProductInput(request: protos.google.shopping.merchant.products.v1.IInsertProductInputRequest, options: CallOptions, callback: Callback<protos.google.shopping.merchant.products.v1.IProductInput, protos.google.shopping.merchant.products.v1.IInsertProductInputRequest | null | undefined, {} | null | undefined>): void;
    insertProductInput(request: protos.google.shopping.merchant.products.v1.IInsertProductInputRequest, callback: Callback<protos.google.shopping.merchant.products.v1.IProductInput, protos.google.shopping.merchant.products.v1.IInsertProductInputRequest | null | undefined, {} | null | undefined>): void;
    /**
     * Updates the existing product input in your Merchant Center account.
     * The name of the product input to update is taken from the `name` field
     * within the `ProductInput` resource.
     *
     * After inserting, updating, or deleting a product input, it may take several
     * minutes before the processed product can be retrieved.
     *
     * @param {Object} request
     *   The request object that will be sent.
     * @param {google.shopping.merchant.products.v1.ProductInput} request.productInput
     *   Required. The product input resource to update. Information you submit will
     *   be applied to the processed product as well. The `name` field within this
     *   resource identifies the product input to be updated.
     * @param {google.protobuf.FieldMask} [request.updateMask]
     *   Optional. The list of product attributes to be updated.
     *
     *   If the update mask is omitted, then it is treated as implied field mask
     *   equivalent to all fields that are populated (have a non-empty value).
     *
     *   Attributes specified in the update mask without a value specified in the
     *   body will be deleted from the product.
     *
     *   Update mask can only be specified for top level fields in
     *   attributes and custom attributes.
     *
     *   To specify the update mask for custom attributes you need to add the
     *   `custom_attribute.` prefix.
     *
     *   Providing special "*" value for full product replacement is not supported.
     * @param {string} request.dataSource
     *   Required. The primary or supplemental product data source where
     *   `data_source` name identifies the product input to be updated.
     *
     *   Only API data sources are supported.
     *
     *   Format: `accounts/{account}/dataSources/{datasource}`. For example,
     *   `accounts/123456/dataSources/104628`.
     * @param {object} [options]
     *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
     * @returns {Promise} - The promise which resolves to an array.
     *   The first element of the array is an object representing {@link protos.google.shopping.merchant.products.v1.ProductInput|ProductInput}.
     *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods | documentation }
     *   for more details and examples.
     * @example <caption>include:samples/generated/v1/product_inputs_service.update_product_input.js</caption>
     * region_tag:merchantapi_v1_generated_ProductInputsService_UpdateProductInput_async
     */
    updateProductInput(request?: protos.google.shopping.merchant.products.v1.IUpdateProductInputRequest, options?: CallOptions): Promise<[
        protos.google.shopping.merchant.products.v1.IProductInput,
        protos.google.shopping.merchant.products.v1.IUpdateProductInputRequest | undefined,
        {} | undefined
    ]>;
    updateProductInput(request: protos.google.shopping.merchant.products.v1.IUpdateProductInputRequest, options: CallOptions, callback: Callback<protos.google.shopping.merchant.products.v1.IProductInput, protos.google.shopping.merchant.products.v1.IUpdateProductInputRequest | null | undefined, {} | null | undefined>): void;
    updateProductInput(request: protos.google.shopping.merchant.products.v1.IUpdateProductInputRequest, callback: Callback<protos.google.shopping.merchant.products.v1.IProductInput, protos.google.shopping.merchant.products.v1.IUpdateProductInputRequest | null | undefined, {} | null | undefined>): void;
    /**
     * Deletes a product input from your Merchant Center account.
     *
     * After inserting, updating, or deleting a product input, it may take several
     * minutes before the processed product can be retrieved.
     *
     * @param {Object} request
     *   The request object that will be sent.
     * @param {string} request.name
     *   Required. The name of the product input to delete.
     *   Format: `accounts/{account}/productInputs/{productInput}`
     *
     *   The {productInput} segment is a unique identifier for the product.
     *   This identifier must be unique within a merchant account and generally
     *   follows the structure: `content_language~feed_label~offer_id`. Example:
     *   `en~US~sku123` For legacy local products, the structure is:
     *   `local~content_language~feed_label~offer_id`. Example: `local~en~US~sku123`
     *
     *   The format of the {productInput} segment in the URL is automatically
     *   detected by the server, supporting two options:
     *
     *   1.  **Encoded Format**: The `{productInput}` segment is an unpadded
     *   base64url
     *       encoded string (RFC 4648 Section 5). The decoded string must result
     *       in the `content_language~feed_label~offer_id` structure. This encoding
     *       MUST be used if any part of the product identifier (like `offer_id`)
     *       contains characters such as `/`, `%`, or `~`.
     *       *   Example: To represent the product ID `en~US~sku/123`, the
     *           `{productInput}` segment must be the unpadded base64url encoding of
     *           this string, which is `ZW5-VVN-c2t1LzEyMw`. The full resource name
     *           for the product would be
     *           `accounts/123/productInputs/ZW5-VVN-c2t1LzEyMw`.
     *
     *   2.  **Plain Format**: The `{productInput}` segment is the tilde-separated
     *   string
     *       `content_language~feed_label~offer_id`. This format is suitable only
     *       when `content_language`, `feed_label`, and `offer_id` do not contain
     *       URL-problematic characters like `/`, `%`, or `~`.
     *
     *   We recommend using the **Encoded Format** for all product IDs to ensure
     *   correct parsing, especially those containing special characters. The
     *   presence of tilde (`~`) characters in the `{productInput}` segment is used
     *   to differentiate between the two formats.
     * @param {string} request.dataSource
     *   Required. The primary or supplemental data source from which the product
     *   input should be deleted. Format:
     *   `accounts/{account}/dataSources/{datasource}`. For example,
     *   `accounts/123456/dataSources/104628`.
     * @param {object} [options]
     *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
     * @returns {Promise} - The promise which resolves to an array.
     *   The first element of the array is an object representing {@link protos.google.protobuf.Empty|Empty}.
     *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods | documentation }
     *   for more details and examples.
     * @example <caption>include:samples/generated/v1/product_inputs_service.delete_product_input.js</caption>
     * region_tag:merchantapi_v1_generated_ProductInputsService_DeleteProductInput_async
     */
    deleteProductInput(request?: protos.google.shopping.merchant.products.v1.IDeleteProductInputRequest, options?: CallOptions): Promise<[
        protos.google.protobuf.IEmpty,
        protos.google.shopping.merchant.products.v1.IDeleteProductInputRequest | undefined,
        {} | undefined
    ]>;
    deleteProductInput(request: protos.google.shopping.merchant.products.v1.IDeleteProductInputRequest, options: CallOptions, callback: Callback<protos.google.protobuf.IEmpty, protos.google.shopping.merchant.products.v1.IDeleteProductInputRequest | null | undefined, {} | null | undefined>): void;
    deleteProductInput(request: protos.google.shopping.merchant.products.v1.IDeleteProductInputRequest, callback: Callback<protos.google.protobuf.IEmpty, protos.google.shopping.merchant.products.v1.IDeleteProductInputRequest | null | undefined, {} | null | undefined>): void;
    /**
     * Return a fully-qualified account resource name string.
     *
     * @param {string} account
     * @returns {string} Resource name string.
     */
    accountPath(account: string): string;
    /**
     * Parse the account from Account resource.
     *
     * @param {string} accountName
     *   A fully-qualified path representing Account resource.
     * @returns {string} A string representing the account.
     */
    matchAccountFromAccountName(accountName: string): string | number;
    /**
     * Return a fully-qualified product resource name string.
     *
     * @param {string} account
     * @param {string} product
     * @returns {string} Resource name string.
     */
    productPath(account: string, product: string): string;
    /**
     * Parse the account from Product resource.
     *
     * @param {string} productName
     *   A fully-qualified path representing Product resource.
     * @returns {string} A string representing the account.
     */
    matchAccountFromProductName(productName: string): string | number;
    /**
     * Parse the product from Product resource.
     *
     * @param {string} productName
     *   A fully-qualified path representing Product resource.
     * @returns {string} A string representing the product.
     */
    matchProductFromProductName(productName: string): string | number;
    /**
     * Return a fully-qualified productInput resource name string.
     *
     * @param {string} account
     * @param {string} productinput
     * @returns {string} Resource name string.
     */
    productInputPath(account: string, productinput: string): string;
    /**
     * Parse the account from ProductInput resource.
     *
     * @param {string} productInputName
     *   A fully-qualified path representing ProductInput resource.
     * @returns {string} A string representing the account.
     */
    matchAccountFromProductInputName(productInputName: string): string | number;
    /**
     * Parse the productinput from ProductInput resource.
     *
     * @param {string} productInputName
     *   A fully-qualified path representing ProductInput resource.
     * @returns {string} A string representing the productinput.
     */
    matchProductinputFromProductInputName(productInputName: string): string | number;
    /**
     * Terminate the gRPC channel and close the client.
     *
     * The client will no longer be usable and all future behavior is undefined.
     * @returns {Promise} A promise that resolves when the client is closed.
     */
    close(): Promise<void>;
}
